/**
 * Completion cache entries
 *
 * Simple string arrays for shell completion.
 * No timestamps - file order represents recency (newest at end).
 */

/**
 * Alias values for completion
 */
export type AliasCompletionEntry = string;

/**
 * Context tag values for completion
 */
export type ContextTagCompletionEntry = string;
